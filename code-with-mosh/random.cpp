#include<bits/stdc++.h>
using namespace std;

int main(){
    // long elapsedSeconds = time(0);
    // srand(elapsedSeconds);
    srand(time(0));
    int number = rand() % 10;
    cout<<number;
    return 0;
}